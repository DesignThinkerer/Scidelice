import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.scidelice',
  appName: 'scidelice',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
